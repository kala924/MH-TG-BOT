module.exports = async function chatWithAI(message, options = {}) {
  /*
    AI engine will be connected here.

    options:
      - userId
      - language
      - conversation
  */

  return "🤖 AI engine is not connected yet.";
};
